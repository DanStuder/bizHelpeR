% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/farbvorschau.R
\name{farbvorschau}
\alias{farbvorschau}
\title{Farbenvorschau plotten}
\usage{
farbvorschau(colors, rgb_cols = NULL)
}
\arguments{
\item{colors}{Vektor, Liste oder Data Frame mit Farbwerten.
\itemize{
\item Falls Data Frame, kann mit \code{rgb_cols} die RGB-Spalten angegeben werden.
\item Unterstützt HEX-Codes (z.B. \code{"#RRGGBB"} oder \code{"#RRGGBBAA"}).
\item RGB als drei Spalten mit Werten 0 bis 255 möglich.
}}

\item{rgb_cols}{Optional. Charaktervektor mit Namen der RGB-Spalten (z.B. \code{c("r","g","b")}),
wenn \code{colors} ein Data Frame mit RGB-Werten ist.}
}
\value{
Ein ggplot2-Plot mit der Farbpalette als Kacheln.
}
\description{
Diese Funktion zeigt eine Farbpalette als Kacheln an und erlaubt als Input
Vektoren, Listen oder Data Frames mit HEX- oder RGB-Farben (auch mit Alphakanal).
Farben werden validiert und nicht passende Werte werden ignoriert.
}
\examples{
farbvorschau(c("#00000040", "#a9bcc5FF", "#7d9aa9CC"))
df_rgb <- data.frame(r = c(169, 125, 213), g = c(188, 154, 239), b = c(197, 169, 249))
farbvorschau(df_rgb, rgb_cols = c("r", "g", "b"))

}
